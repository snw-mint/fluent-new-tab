{
  "name": "fluent-new-tab",
  "version": "4.5",
  "private": true,
  "description": "Build tooling for Fluent New Tab (browser extension)",
  "scripts": {
    "clean": "rimraf dist",
    "dev": "vite",
    "build": "npm run clean && vite build"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@types/chrome": "^0.1.42",
    "@types/node": "^25.9.1",
    "esbuild": "^0.28.1",
    "rimraf": "^6.0.1",
    "sass": "^1.92.1",
    "terser": "^5.44.0",
    "ts-morph": "^28.0.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.16"
  },
  "overrides": {
    "@crxjs/vite-plugin": {
      "rollup": "^2.80.0"
    }
  }
}
